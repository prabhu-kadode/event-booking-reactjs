import CreateEvent from './../components/Create-Event';
const Event = ({token}) => {
    return (
        <div>
            <CreateEvent token = {token}/>
        </div>
    )
}
 export default Event;